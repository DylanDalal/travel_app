specVersion: "v1beta"
serviceId: "passport"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "passport-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
